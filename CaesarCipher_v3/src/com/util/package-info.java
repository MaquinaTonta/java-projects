/**
 * Provides the class <code>RepeatedCharacters</code> which is used to detect 
 * if a character is repeated when the alphabet is loaded to the 
 * <code>Caesar</code> class.
 */
package com.util;